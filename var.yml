- name: Practice Print statement
  hosts: localhost
  tasks:
    - name: print my name
      ansible.builtin.debug:
        msg: My name is Varnita Rajesh Fagade

- name: Practice task level variable
  hosts: localhost
  vars:
    NAME: VARNITARAJEHFEGADE
  tasks:
    - name: task level
      ansible.builtin.debug:
        msg: "{{ NAME }}"
      vars:
          NAME: MrunmayiFegade

    - name: Practice play level variable
      ansible.builtin.debug:
        msg: "{{ NAME }}"


